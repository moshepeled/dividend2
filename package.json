{
  "name": "express_mLab",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd react_exp_mlab && npm run build",
    "rebuildStart": "cd react_exp_mlab && npm run build && cd .. && node server.js",
    "heroku-postbuild": "cd react_exp_mlab && npm install && npm run build",
    "heroku": "git push heroku master"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "mongodb": "^2.2.35",
    "mongoose": "^5.0.12"
  }
}
